{
  "name": "@ddas/cli",
  "version": "2.0.0-alpha.0",
  "description": "The ddas CLI: register policies, submit transactions, classify, simulate policy drafts, run the eval harness",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ddas": "./dist/main.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ddas/engine": "workspace:*",
    "@ddas/extraction": "workspace:*",
    "@ddas/policy": "workspace:*",
    "@ddas/testkit": "workspace:*",
    "commander": "^13.1.0",
    "@ddas/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
