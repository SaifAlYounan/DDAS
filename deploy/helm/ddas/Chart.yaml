apiVersion: v2
name: ddas
description: LQGovernance - DDAS (Dynamic Delegation of Authority System) — reference chart, single replica by default and HA-ready via replicaCount (bring your own Postgres)
type: application
version: 0.3.0
appVersion: "2.0.0"
