apiVersion: v2
name: ddas
description: Dynamic Delegation of Authority System — reference chart, single replica by default and HA-ready via replicaCount (bring your own Postgres)
type: application
version: 0.2.0
appVersion: "2.0.0-alpha.0"
