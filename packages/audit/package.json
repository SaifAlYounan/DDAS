{
  "name": "@ddas/audit",
  "version": "2.0.0",
  "description": "DDAS append-only audit chain: hash-linked events, streaming verifier, exportable checkpoints",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ddas/policy": "workspace:*",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@ddas/db": "workspace:*",
    "@types/node": "^24.13.2",
    "@types/pg": "^8.15.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
