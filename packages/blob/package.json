{
  "name": "@ddas/blob",
  "version": "2.0.0-alpha.0",
  "description": "DDAS blob storage drivers: content-addressed document blobs on the local filesystem or any S3-compatible store, behind one narrow interface",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.828.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
