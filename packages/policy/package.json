{
  "name": "@ddas/policy",
  "version": "2.0.0",
  "description": "DDAS policy-as-code: the RiskPolicy schema, YAML→canonical-JSON compiler, semantic linter, and content hashing",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "schema",
    "templates"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
